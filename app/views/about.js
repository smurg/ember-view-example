import Ember from 'ember';

export default Ember.View.extend({
  tagName: 'div',
  classNames: ['jumbotron']
    /* In this view we define the container HTML type (div, section, nav) 
    and the bootstrap class that container has. */
});